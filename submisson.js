function showResponse(event) {
    var response =
    "thank you for your completion " + "nameSubmit.value";
    submitResponseEl.text.Content = response; 

};

submitEl.addEventListener("click", preventDefault());
